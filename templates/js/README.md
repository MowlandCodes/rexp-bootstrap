# __PROJECT_NAME__

React + Express + Prisma full-stack monorepo

## Getting started

```bash
__PKG_INSTALL__
__PKG_RUN__ dev
```

## Workspaces

- `client/` — Vite + React + Tailwind CSS v4
- `server/` — Express + Prisma ORM v7
